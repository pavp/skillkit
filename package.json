{
  "name": "skillkit",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/build.js",
  "scripts": {
    "compile": "tsc",
    "build": "tsc && node dist/build.js",
    "build:dry": "tsc && node dist/build.js --dry-run",
    "start": "node dist/build.js",
    "test": "tsc -p tsconfig.test.json && node --test \"dist-test/test/**/*.test.js\""
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "yaml": "^2.7.1"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.8.3"
  }
}

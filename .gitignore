# --- gentle-ai / tooling-generated (local, regenerated, not source) ---
# Engram local project marker (machine-specific)
.engram/
# Auto-generated skill registry index (regenerated by gentle-ai)
.atl/

# --- skills.sh ---
# Lockfile is per-install-target; skills install via `npx skills add`.
skills-lock.json
# Agent install dirs created when installing into this repo as a project.
.claude/
.agents/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.idea/
*.swp
